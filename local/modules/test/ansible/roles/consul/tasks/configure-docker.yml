- name: "Copy docker file to /etc/default/docker"
  template:
    src: templates/docker/docker.j2
    dest: /etc/default/docker
    owner: root
    group: root
    mode: 0644

- name: "Restart docker service"
  service:
    name: docker
    state: restarted
